echo Do you really want to log off?
echo Enter nothing to stay logged in, something to logoff
read answer

if test -z "$answer" # -z checks if the string is empty
then
    echo "I am glad you are staying"
else
    exit 0
fi
